# Python program to read
# json file


import json

# Opening JSON file
f = open('data.json',)

# returns JSON object as
# a dictionary
data = json.load(f)

# Iterating through the json
# list
for i in data['seo_category_infos']:
	print(i)

for j in data['logging_page_id']:
  print (j)

# Closing file
f.close()
